import React from 'react';
import { Header } from '../../components/Header';
import { Card, Cards, CardTitle, Container } from './styles';

export default function Categories() {
  return (
    <Container>
      <Header title="Categories" description="Thousands of articles in each category" />
    </Container>
  )
};